import { CurrencyNumericInputDirective } from './currency-numeric-input.directive';

describe('CurrencyNumericInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyNumericInputDirective();
    expect(directive).toBeTruthy();
  });
});
